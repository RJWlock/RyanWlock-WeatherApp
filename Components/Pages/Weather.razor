@page "/weather"
@rendermode InteractiveServer

<PageTitle>Weather</PageTitle>

<MudText Typo="Typo.h3" GutterBottom="true">
    Weather Search
</MudText>

<MudText Typo="Typo.body1" Class="mb-4">
    Enter a U.S. state abbreviation to load forecast zones.
</MudText>

<MudStack Row="true" Spacing="2" Class="mb-4">
    <MudTextField @bind-Value="stateInput"
                  Label="State"
                  Placeholder="CO"
                  Variant="Variant.Outlined" />

    <MudButton Variant="Variant.Filled"
               Color="Color.Primary"
               OnClick="SearchZones">
        Search
    </MudButton>
</MudStack>

@if (isLoadingZones)
{
    <MudProgressCircular Indeterminate="true" Class="my-4" />
}

@if (!string.IsNullOrWhiteSpace(zonesError))
{
    <MudAlert Severity="Severity.Error" Class="my-4">
        @zonesError
    </MudAlert>
}

@if (!string.IsNullOrWhiteSpace(zonesEmpty))
{
    <MudAlert Severity="Severity.Info" Class="my-4">
        @zonesEmpty
    </MudAlert>
}

@if (zones is not null)
{
    <MudTable Items="zones"
              Hover="true"
              Dense="true"
              RowsPerPage="10">
        <HeaderContent>
            <MudTh>Zone ID</MudTh>
            <MudTh>Name</MudTh>
            <MudTh>Action</MudTh>
        </HeaderContent>

        <RowTemplate>
            <MudTd>@context.Properties.Id</MudTd>
            <MudTd>@context.Properties.Name</MudTd>
            <MudTd>
                <MudButton Variant="Variant.Outlined"
                           Color="Color.Primary"
                           OnClick="@(() => SelectZone(context))">
                    Select
                </MudButton>
            </MudTd>
        </RowTemplate>

        <ChildRowContent>
            @if (selectedZoneId == context.Properties.Id)
            {
                <MudTd colSpan="3">
                    <MudPaper Class="pa-4 my-2" Elevation="0">
                        <MudText Typo="Typo.h6" Class="mb-2">
                            Stations for @context.Properties.Name
                        </MudText>

                        @if (isLoadingStations)
                        {
                            <MudProgressCircular Indeterminate="true" Class="my-2" />
                        }

                        @if (!string.IsNullOrWhiteSpace(stationsError))
                        {
                            <MudAlert Severity="Severity.Error" Class="my-2">
                                @stationsError
                            </MudAlert>
                        }

                        @if (!string.IsNullOrWhiteSpace(stationsEmpty))
                        {
                            <MudAlert Severity="Severity.Info" Class="my-2">
                                @stationsEmpty
                            </MudAlert>
                        }

                        @if (stations is not null)
                        {
                            <MudTable Items="stations" Dense="true" Hover="true">
                                <HeaderContent>
                                    <MudTh>Station ID</MudTh>
                                    <MudTh>Name</MudTh>
                                    <MudTh>Action</MudTh>
                                </HeaderContent>

                                <RowTemplate Context="station">
                                    <MudTd>@station.Properties.StationIdentifier</MudTd>
                                    <MudTd>@station.Properties.Name</MudTd>
                                    <MudTd>
                                        <MudButton Variant="Variant.Filled"
                                                   Color="Color.Secondary"
                                                   OnClick="@(() => GoToForecastPage(station))">
                                            Forecast
                                        </MudButton>
                                    </MudTd>
                                </RowTemplate>
                            </MudTable>
                        }
                    </MudPaper>
                </MudTd>
            }
        </ChildRowContent>
    </MudTable>
}